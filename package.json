{
  "name": "bushy",
  "version": "1.0.0",
  "description": "A webcam photobooth that grows you a unibrow built from your own hair.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "start": "node -e \"import('./tests/server.mjs').then(async m=>{const s=await m.serve(process.cwd());console.log(s.origin)})\" "
  },
  "devDependencies": {
    "playwright": "^1.62.0"
  }
}
